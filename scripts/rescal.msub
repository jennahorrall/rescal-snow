#!/bin/bash
#MSUB -A intelpwr
#MSUB -q pbatch
#MSUB -m abe
#MSUB -M horrall1@llnl.gov
#MSUB -l partition=quartz
#MSUB -l nodes=1
#MSUB -V
#MSUB -l walltime=00:03:00

echo "starting to run"

# Job file submitted via "msub <filename>".
# In this case, N tasks are launched by srun, each of which executes
#  ./rescal.sh
# (N is set as --ntasks=N)

# This file will run N tasks, with PMI ranks 0 to N

srun --nodes=1 --cpus-per-task=1 --ntasks=3 --wait=0 submit.sh
